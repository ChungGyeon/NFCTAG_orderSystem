<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>TagOrder 테스트 페이지</title>
    <link href="/TestStore/TestStore_admin/Order_related_page/testStorescss.css" rel="stylesheet">
    <script defer src="/TestStore/test_js.js"></script>
</head>
<body>
<div class="test-container">
    <header>
        <h1>TagOrder</h1>
    </header>
    <main id="main-content">
        <% orders.forEach(order => { %>
        <!--data-table은 손님쪽에서 id를 구현 후 제작해야해-->
        <div class="table-card" data-table="1">
            <h3>테이블 1</h3>
            <ul>
                <li>
                    <h2>메뉴: <%= order.menu %></h2>
                    <p>옵션: <%= order.options.map(option => option.name).join(', ') %></p>
                </li>
            </ul>
            <label><input type="checkbox" class="table-check"> 정산 대상 선택</label>
        </div>
        <% }) %>
    </main>
    <aside>
        <h3>사이드바</h3>
        <p>추가 정보</p>
        <button class="settle-button" onclick="openModal()">정산하기</button>
    </aside>
</div>

<div id="settle-modal" class="modal-overlay">
    <div class="modal-content">
        <h2>정산 결과</h2>
        <div id="settle-result"></div>
        <button onclick="closeModal()">닫기</button>
    </div>
</div>
</body>
</html>